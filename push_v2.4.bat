@echo off
echo v2.4: Instagram carousel, MoviePy fix, Admin ID fix
git add .
git commit -m "v2.4: Instagram carousel support, Fix MoviePy import, Fix Admin ID parsing, Simplify MP3 cleanup"
git push origin main
pause
